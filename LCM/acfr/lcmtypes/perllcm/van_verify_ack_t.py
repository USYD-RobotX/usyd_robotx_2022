"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class van_verify_ack_t(object):
    __slots__ = ["utime1", "utime2", "valid", "stop_verifying"]

    def __init__(self):
        self.utime1 = 0
        self.utime2 = 0
        self.valid = False
        self.stop_verifying = False

    def encode(self):
        buf = BytesIO()
        buf.write(van_verify_ack_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qqbb", self.utime1, self.utime2, self.valid, self.stop_verifying))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != van_verify_ack_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return van_verify_ack_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = van_verify_ack_t()
        self.utime1, self.utime2 = struct.unpack(">qq", buf.read(16))
        self.valid = bool(struct.unpack('b', buf.read(1))[0])
        self.stop_verifying = bool(struct.unpack('b', buf.read(1))[0])
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if van_verify_ack_t in parents: return 0
        tmphash = (0x19c7a7698198e530) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if van_verify_ack_t._packed_fingerprint is None:
            van_verify_ack_t._packed_fingerprint = struct.pack(">Q", van_verify_ack_t._get_hash_recursive([]))
        return van_verify_ack_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


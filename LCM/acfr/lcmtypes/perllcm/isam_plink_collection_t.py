"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import perllcm.isam_plink_t

class isam_plink_collection_t(object):
    __slots__ = ["utime", "nlinks", "plink"]

    def __init__(self):
        self.utime = 0
        self.nlinks = 0
        self.plink = []

    def encode(self):
        buf = BytesIO()
        buf.write(isam_plink_collection_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qi", self.utime, self.nlinks))
        for i0 in range(self.nlinks):
            assert self.plink[i0]._get_packed_fingerprint() == perllcm.isam_plink_t._get_packed_fingerprint()
            self.plink[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != isam_plink_collection_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return isam_plink_collection_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = isam_plink_collection_t()
        self.utime, self.nlinks = struct.unpack(">qi", buf.read(12))
        self.plink = []
        for i0 in range(self.nlinks):
            self.plink.append(perllcm.isam_plink_t._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if isam_plink_collection_t in parents: return 0
        newparents = parents + [isam_plink_collection_t]
        tmphash = (0x9dbb356a41d4144d+ perllcm.isam_plink_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if isam_plink_collection_t._packed_fingerprint is None:
            isam_plink_collection_t._packed_fingerprint = struct.pack(">Q", isam_plink_collection_t._get_hash_recursive([]))
        return isam_plink_collection_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import perllcm.est_navigator_index_t

class segway_navigator_t(object):
    __slots__ = ["utime", "est_method", "init_data_ready", "filter_running", "state_len", "delayed_state_len", "max_delayed_states", "delayed_states_cnt", "delayed_states_utime", "mu_len", "Sigma_len", "mu", "Sigma", "index", "org_latitude", "org_longitude"]

    EST_EKF = 1
    EST_UKF = 2
    EST_PF = 3

    def __init__(self):
        self.utime = 0
        self.est_method = 0
        self.init_data_ready = False
        self.filter_running = False
        self.state_len = 0
        self.delayed_state_len = 0
        self.max_delayed_states = 0
        self.delayed_states_cnt = 0
        self.delayed_states_utime = []
        self.mu_len = 0
        self.Sigma_len = 0
        self.mu = []
        self.Sigma = []
        self.index = perllcm.est_navigator_index_t()
        self.org_latitude = 0.0
        self.org_longitude = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(segway_navigator_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qhbbiiii", self.utime, self.est_method, self.init_data_ready, self.filter_running, self.state_len, self.delayed_state_len, self.max_delayed_states, self.delayed_states_cnt))
        buf.write(struct.pack('>%dq' % self.delayed_states_cnt, *self.delayed_states_utime[:self.delayed_states_cnt]))
        buf.write(struct.pack(">ii", self.mu_len, self.Sigma_len))
        buf.write(struct.pack('>%dd' % self.mu_len, *self.mu[:self.mu_len]))
        buf.write(struct.pack('>%dd' % self.Sigma_len, *self.Sigma[:self.Sigma_len]))
        assert self.index._get_packed_fingerprint() == perllcm.est_navigator_index_t._get_packed_fingerprint()
        self.index._encode_one(buf)
        buf.write(struct.pack(">dd", self.org_latitude, self.org_longitude))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != segway_navigator_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return segway_navigator_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = segway_navigator_t()
        self.utime, self.est_method = struct.unpack(">qh", buf.read(10))
        self.init_data_ready = bool(struct.unpack('b', buf.read(1))[0])
        self.filter_running = bool(struct.unpack('b', buf.read(1))[0])
        self.state_len, self.delayed_state_len, self.max_delayed_states, self.delayed_states_cnt = struct.unpack(">iiii", buf.read(16))
        self.delayed_states_utime = struct.unpack('>%dq' % self.delayed_states_cnt, buf.read(self.delayed_states_cnt * 8))
        self.mu_len, self.Sigma_len = struct.unpack(">ii", buf.read(8))
        self.mu = struct.unpack('>%dd' % self.mu_len, buf.read(self.mu_len * 8))
        self.Sigma = struct.unpack('>%dd' % self.Sigma_len, buf.read(self.Sigma_len * 8))
        self.index = perllcm.est_navigator_index_t._decode_one(buf)
        self.org_latitude, self.org_longitude = struct.unpack(">dd", buf.read(16))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if segway_navigator_t in parents: return 0
        newparents = parents + [segway_navigator_t]
        tmphash = (0xb8eaa6b3ec138a55+ perllcm.est_navigator_index_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if segway_navigator_t._packed_fingerprint is None:
            segway_navigator_t._packed_fingerprint = struct.pack(">Q", segway_navigator_t._get_hash_recursive([]))
        return segway_navigator_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class logbook_t(object):
    __slots__ = ["utime", "username", "hostname", "message"]

    def __init__(self):
        self.utime = 0
        self.username = ""
        self.hostname = ""
        self.message = ""

    def encode(self):
        buf = BytesIO()
        buf.write(logbook_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">q", self.utime))
        __username_encoded = self.username.encode('utf-8')
        buf.write(struct.pack('>I', len(__username_encoded)+1))
        buf.write(__username_encoded)
        buf.write(b"\0")
        __hostname_encoded = self.hostname.encode('utf-8')
        buf.write(struct.pack('>I', len(__hostname_encoded)+1))
        buf.write(__hostname_encoded)
        buf.write(b"\0")
        __message_encoded = self.message.encode('utf-8')
        buf.write(struct.pack('>I', len(__message_encoded)+1))
        buf.write(__message_encoded)
        buf.write(b"\0")

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != logbook_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return logbook_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = logbook_t()
        self.utime = struct.unpack(">q", buf.read(8))[0]
        __username_len = struct.unpack('>I', buf.read(4))[0]
        self.username = buf.read(__username_len)[:-1].decode('utf-8', 'replace')
        __hostname_len = struct.unpack('>I', buf.read(4))[0]
        self.hostname = buf.read(__hostname_len)[:-1].decode('utf-8', 'replace')
        __message_len = struct.unpack('>I', buf.read(4))[0]
        self.message = buf.read(__message_len)[:-1].decode('utf-8', 'replace')
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if logbook_t in parents: return 0
        tmphash = (0x3291dbeba043fefa) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if logbook_t._packed_fingerprint is None:
            logbook_t._packed_fingerprint = struct.pack(">Q", logbook_t._get_hash_recursive([]))
        return logbook_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


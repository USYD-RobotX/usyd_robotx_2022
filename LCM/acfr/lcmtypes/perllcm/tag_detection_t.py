"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import perllcm.position_t

class tag_detection_t(object):
    __slots__ = ["utime", "id", "pose"]

    def __init__(self):
        self.utime = 0
        self.id = 0
        self.pose = perllcm.position_t()

    def encode(self):
        buf = BytesIO()
        buf.write(tag_detection_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qi", self.utime, self.id))
        assert self.pose._get_packed_fingerprint() == perllcm.position_t._get_packed_fingerprint()
        self.pose._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != tag_detection_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return tag_detection_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = tag_detection_t()
        self.utime, self.id = struct.unpack(">qi", buf.read(12))
        self.pose = perllcm.position_t._decode_one(buf)
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if tag_detection_t in parents: return 0
        newparents = parents + [tag_detection_t]
        tmphash = (0xb88af5410b6af4d0+ perllcm.position_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if tag_detection_t._packed_fingerprint is None:
            tag_detection_t._packed_fingerprint = struct.pack(">Q", tag_detection_t._get_hash_recursive([]))
        return tag_detection_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


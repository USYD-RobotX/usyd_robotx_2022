"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class ardrone_cmd_t(object):
    __slots__ = ["utime", "takeoff", "hover", "camera", "emergency", "controller", "auth_follow", "auth_land", "change_speed"]

    def __init__(self):
        self.utime = 0
        self.takeoff = False
        self.hover = False
        self.camera = False
        self.emergency = False
        self.controller = False
        self.auth_follow = False
        self.auth_land = False
        self.change_speed = 0

    def encode(self):
        buf = BytesIO()
        buf.write(ardrone_cmd_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qbbbbbbbi", self.utime, self.takeoff, self.hover, self.camera, self.emergency, self.controller, self.auth_follow, self.auth_land, self.change_speed))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ardrone_cmd_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ardrone_cmd_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = ardrone_cmd_t()
        self.utime = struct.unpack(">q", buf.read(8))[0]
        self.takeoff = bool(struct.unpack('b', buf.read(1))[0])
        self.hover = bool(struct.unpack('b', buf.read(1))[0])
        self.camera = bool(struct.unpack('b', buf.read(1))[0])
        self.emergency = bool(struct.unpack('b', buf.read(1))[0])
        self.controller = bool(struct.unpack('b', buf.read(1))[0])
        self.auth_follow = bool(struct.unpack('b', buf.read(1))[0])
        self.auth_land = bool(struct.unpack('b', buf.read(1))[0])
        self.change_speed = struct.unpack(">i", buf.read(4))[0]
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if ardrone_cmd_t in parents: return 0
        tmphash = (0x8a8f738fc4b62934) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if ardrone_cmd_t._packed_fingerprint is None:
            ardrone_cmd_t._packed_fingerprint = struct.pack(">Q", ardrone_cmd_t._get_hash_recursive([]))
        return ardrone_cmd_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


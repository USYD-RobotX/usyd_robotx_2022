"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class van_timer_t(object):
    __slots__ = ["event_id", "type", "t_now", "dt"]

    def __init__(self):
        self.event_id = 0
        self.type = 0
        self.t_now = 0
        self.dt = 0

    def encode(self):
        buf = BytesIO()
        buf.write(van_timer_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qiqq", self.event_id, self.type, self.t_now, self.dt))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != van_timer_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return van_timer_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = van_timer_t()
        self.event_id, self.type, self.t_now, self.dt = struct.unpack(">qiqq", buf.read(28))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if van_timer_t in parents: return 0
        tmphash = (0xb0c5a40f66716bb0) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if van_timer_t._packed_fingerprint is None:
            van_timer_t._packed_fingerprint = struct.pack(">Q", van_timer_t._get_hash_recursive([]))
        return van_timer_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


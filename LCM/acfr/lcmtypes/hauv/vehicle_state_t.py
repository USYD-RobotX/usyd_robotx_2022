"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class vehicle_state_t(object):
    __slots__ = ["time", "x", "y", "z", "roll", "pitch", "heading", "altitude", "dvlAngle", "sonarAngle", "rmin", "rmax", "sonarX", "sonarY", "sonarZ"]

    def __init__(self):
        self.time = 0
        self.x = 0.0
        self.y = 0.0
        self.z = 0.0
        self.roll = 0.0
        self.pitch = 0.0
        self.heading = 0.0
        self.altitude = 0.0
        self.dvlAngle = 0.0
        self.sonarAngle = 0.0
        self.rmin = 0.0
        self.rmax = 0.0
        self.sonarX = 0.0
        self.sonarY = 0.0
        self.sonarZ = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(vehicle_state_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qdddddddddddddd", self.time, self.x, self.y, self.z, self.roll, self.pitch, self.heading, self.altitude, self.dvlAngle, self.sonarAngle, self.rmin, self.rmax, self.sonarX, self.sonarY, self.sonarZ))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != vehicle_state_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return vehicle_state_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = vehicle_state_t()
        self.time, self.x, self.y, self.z, self.roll, self.pitch, self.heading, self.altitude, self.dvlAngle, self.sonarAngle, self.rmin, self.rmax, self.sonarX, self.sonarY, self.sonarZ = struct.unpack(">qdddddddddddddd", buf.read(120))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if vehicle_state_t in parents: return 0
        tmphash = (0xe28cbbfc2a823e4a) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if vehicle_state_t._packed_fingerprint is None:
            vehicle_state_t._packed_fingerprint = struct.pack(">Q", vehicle_state_t._get_hash_recursive([]))
        return vehicle_state_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


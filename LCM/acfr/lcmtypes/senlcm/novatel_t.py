"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class novatel_t(object):
    __slots__ = ["utime", "gps_time", "latitude", "longitude", "roll", "pitch", "heading", "height", "north_velocity", "east_velocity", "up_velocity", "latitude_sd", "longitude_sd", "status"]

    def __init__(self):
        self.utime = 0
        self.gps_time = 0
        self.latitude = 0.0
        self.longitude = 0.0
        self.roll = 0.0
        self.pitch = 0.0
        self.heading = 0.0
        self.height = 0.0
        self.north_velocity = 0.0
        self.east_velocity = 0.0
        self.up_velocity = 0.0
        self.latitude_sd = 0.0
        self.longitude_sd = 0.0
        self.status = ""

    def encode(self):
        buf = BytesIO()
        buf.write(novatel_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qqddddddddddd", self.utime, self.gps_time, self.latitude, self.longitude, self.roll, self.pitch, self.heading, self.height, self.north_velocity, self.east_velocity, self.up_velocity, self.latitude_sd, self.longitude_sd))
        __status_encoded = self.status.encode('utf-8')
        buf.write(struct.pack('>I', len(__status_encoded)+1))
        buf.write(__status_encoded)
        buf.write(b"\0")

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != novatel_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return novatel_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = novatel_t()
        self.utime, self.gps_time, self.latitude, self.longitude, self.roll, self.pitch, self.heading, self.height, self.north_velocity, self.east_velocity, self.up_velocity, self.latitude_sd, self.longitude_sd = struct.unpack(">qqddddddddddd", buf.read(104))
        __status_len = struct.unpack('>I', buf.read(4))[0]
        self.status = buf.read(__status_len)[:-1].decode('utf-8', 'replace')
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if novatel_t in parents: return 0
        tmphash = (0xdef817b50c2fc7d9) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if novatel_t._packed_fingerprint is None:
            novatel_t._packed_fingerprint = struct.pack(">Q", novatel_t._get_hash_recursive([]))
        return novatel_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


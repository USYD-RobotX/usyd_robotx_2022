"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class oa_t(object):
    __slots__ = ["utime", "forward_distance", "altitude"]

    def __init__(self):
        self.utime = 0
        self.forward_distance = 0.0
        self.altitude = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(oa_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qdd", self.utime, self.forward_distance, self.altitude))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != oa_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return oa_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = oa_t()
        self.utime, self.forward_distance, self.altitude = struct.unpack(">qdd", buf.read(24))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if oa_t in parents: return 0
        tmphash = (0xcdb252156f9be51) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if oa_t._packed_fingerprint is None:
            oa_t._packed_fingerprint = struct.pack(">Q", oa_t._get_hash_recursive([]))
        return oa_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


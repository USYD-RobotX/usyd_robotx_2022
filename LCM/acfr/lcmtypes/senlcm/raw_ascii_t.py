"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class raw_ascii_t(object):
    __slots__ = ["utime", "msg"]

    def __init__(self):
        self.utime = 0
        self.msg = ""

    def encode(self):
        buf = BytesIO()
        buf.write(raw_ascii_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">q", self.utime))
        __msg_encoded = self.msg.encode('utf-8')
        buf.write(struct.pack('>I', len(__msg_encoded)+1))
        buf.write(__msg_encoded)
        buf.write(b"\0")

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != raw_ascii_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return raw_ascii_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = raw_ascii_t()
        self.utime = struct.unpack(">q", buf.read(8))[0]
        __msg_len = struct.unpack('>I', buf.read(4))[0]
        self.msg = buf.read(__msg_len)[:-1].decode('utf-8', 'replace')
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if raw_ascii_t in parents: return 0
        tmphash = (0xea923cbcf3d1354a) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if raw_ascii_t._packed_fingerprint is None:
            raw_ascii_t._packed_fingerprint = struct.pack(">Q", raw_ascii_t._get_hash_recursive([]))
        return raw_ascii_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


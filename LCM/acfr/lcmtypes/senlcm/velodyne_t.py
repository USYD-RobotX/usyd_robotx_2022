"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class velodyne_t(object):
    __slots__ = ["utime", "packet_type", "datalen", "data"]

    TYPE_DATA_PACKET = 1
    TYPE_POSITION_PACKET = 2

    def __init__(self):
        self.utime = 0
        self.packet_type = 0
        self.datalen = 0
        self.data = ""

    def encode(self):
        buf = BytesIO()
        buf.write(velodyne_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qbi", self.utime, self.packet_type, self.datalen))
        buf.write(bytearray(self.data[:self.datalen]))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != velodyne_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return velodyne_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = velodyne_t()
        self.utime, self.packet_type, self.datalen = struct.unpack(">qbi", buf.read(13))
        self.data = buf.read(self.datalen)
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if velodyne_t in parents: return 0
        tmphash = (0x8bcd6874fbc279bc) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if velodyne_t._packed_fingerprint is None:
            velodyne_t._packed_fingerprint = struct.pack(">Q", velodyne_t._get_hash_recursive([]))
        return velodyne_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


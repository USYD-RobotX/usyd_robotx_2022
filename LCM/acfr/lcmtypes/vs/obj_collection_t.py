"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import vs.obj_t

class obj_collection_t(object):
    __slots__ = ["id", "name", "type", "reset", "nobjs", "objs"]

    POSE = 1
    TREE = 2
    SQUARE = 3
    POSE3D = 4
    AXIS3D = 5
    TAG = 6

    def __init__(self):
        self.id = 0
        self.name = ""
        self.type = 0
        self.reset = False
        self.nobjs = 0
        self.objs = []

    def encode(self):
        buf = BytesIO()
        buf.write(obj_collection_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">i", self.id))
        __name_encoded = self.name.encode('utf-8')
        buf.write(struct.pack('>I', len(__name_encoded)+1))
        buf.write(__name_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">hbi", self.type, self.reset, self.nobjs))
        for i0 in range(self.nobjs):
            assert self.objs[i0]._get_packed_fingerprint() == vs.obj_t._get_packed_fingerprint()
            self.objs[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != obj_collection_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return obj_collection_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = obj_collection_t()
        self.id = struct.unpack(">i", buf.read(4))[0]
        __name_len = struct.unpack('>I', buf.read(4))[0]
        self.name = buf.read(__name_len)[:-1].decode('utf-8', 'replace')
        self.type = struct.unpack(">h", buf.read(2))[0]
        self.reset = bool(struct.unpack('b', buf.read(1))[0])
        self.nobjs = struct.unpack(">i", buf.read(4))[0]
        self.objs = []
        for i0 in range(self.nobjs):
            self.objs.append(vs.obj_t._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if obj_collection_t in parents: return 0
        newparents = parents + [obj_collection_t]
        tmphash = (0x207e172f8d8dba1d+ vs.obj_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if obj_collection_t._packed_fingerprint is None:
            obj_collection_t._packed_fingerprint = struct.pack(">Q", obj_collection_t._get_hash_recursive([]))
        return obj_collection_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class points_t(object):
    __slots__ = ["id", "collection", "element_id", "n", "entries"]

    def __init__(self):
        self.id = 0
        self.collection = 0
        self.element_id = 0
        self.n = 0
        self.entries = []

    def encode(self):
        buf = BytesIO()
        buf.write(points_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qiqi", self.id, self.collection, self.element_id, self.n))
        for i0 in range(self.n):
            buf.write(struct.pack('>3d', *self.entries[i0][:3]))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != points_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return points_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = points_t()
        self.id, self.collection, self.element_id, self.n = struct.unpack(">qiqi", buf.read(24))
        self.entries = []
        for i0 in range(self.n):
            self.entries.append(struct.unpack('>3d', buf.read(24)))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if points_t in parents: return 0
        tmphash = (0xfb955457476dbecd) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if points_t._packed_fingerprint is None:
            points_t._packed_fingerprint = struct.pack(">Q", points_t._get_hash_recursive([]))
        return points_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


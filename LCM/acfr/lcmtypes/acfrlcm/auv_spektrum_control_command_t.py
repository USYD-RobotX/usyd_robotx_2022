"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class auv_spektrum_control_command_t(object):
    __slots__ = ["utime", "channels", "values"]

    RC_THROTTLE = 0
    RC_AILERON = 1
    RC_ELEVATOR = 2
    RC_RUDDER = 3
    RC_AUX1 = 4
    RC_GEAR = 5

    def __init__(self):
        self.utime = 0
        self.channels = 0
        self.values = []

    def encode(self):
        buf = BytesIO()
        buf.write(auv_spektrum_control_command_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qb", self.utime, self.channels))
        buf.write(struct.pack('>%dh' % self.channels, *self.values[:self.channels]))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != auv_spektrum_control_command_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return auv_spektrum_control_command_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = auv_spektrum_control_command_t()
        self.utime, self.channels = struct.unpack(">qb", buf.read(9))
        self.values = struct.unpack('>%dh' % self.channels, buf.read(self.channels * 2))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if auv_spektrum_control_command_t in parents: return 0
        tmphash = (0x68472ad6104ab27f) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if auv_spektrum_control_command_t._packed_fingerprint is None:
            auv_spektrum_control_command_t._packed_fingerprint = struct.pack(">Q", auv_spektrum_control_command_t._get_hash_recursive([]))
        return auv_spektrum_control_command_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


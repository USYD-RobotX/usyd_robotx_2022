"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class auv_status_short_t(object):
    __slots__ = ["utime", "status", "target_id", "waypoint", "heading", "roll", "pitch", "depth", "altitude", "latitude", "longitude", "img_count", "charge", "vel"]

    def __init__(self):
        self.utime = 0
        self.status = 0
        self.target_id = 0
        self.waypoint = 0
        self.heading = 0
        self.roll = 0
        self.pitch = 0
        self.depth = 0
        self.altitude = 0
        self.latitude = 0.0
        self.longitude = 0.0
        self.img_count = 0
        self.charge = 0
        self.vel = 0

    def encode(self):
        buf = BytesIO()
        buf.write(auv_status_short_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qhbbbbbhbffhbb", self.utime, self.status, self.target_id, self.waypoint, self.heading, self.roll, self.pitch, self.depth, self.altitude, self.latitude, self.longitude, self.img_count, self.charge, self.vel))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != auv_status_short_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return auv_status_short_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = auv_status_short_t()
        self.utime, self.status, self.target_id, self.waypoint, self.heading, self.roll, self.pitch, self.depth, self.altitude, self.latitude, self.longitude, self.img_count, self.charge, self.vel = struct.unpack(">qhbbbbbhbffhbb", buf.read(30))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if auv_status_short_t in parents: return 0
        tmphash = (0x7ae61dac5c56fdf1) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if auv_status_short_t._packed_fingerprint is None:
            auv_status_short_t._packed_fingerprint = struct.pack(">Q", auv_status_short_t._get_hash_recursive([]))
        return auv_status_short_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


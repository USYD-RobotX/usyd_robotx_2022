"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class seabotix_command_t(object):
    __slots__ = ["utime", "mode", "heading", "depth", "altitude"]

    DEPTH_MODE = 0x01
    ALTITUDE_MODE = 0x02
    HEADING_MODE = 0x04

    def __init__(self):
        self.utime = 0
        self.mode = 0
        self.heading = 0.0
        self.depth = 0.0
        self.altitude = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(seabotix_command_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qbddd", self.utime, self.mode, self.heading, self.depth, self.altitude))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != seabotix_command_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return seabotix_command_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = seabotix_command_t()
        self.utime, self.mode, self.heading, self.depth, self.altitude = struct.unpack(">qbddd", buf.read(33))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if seabotix_command_t in parents: return 0
        tmphash = (0xfcffab1f6f5b2bc7) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if seabotix_command_t._packed_fingerprint is None:
            seabotix_command_t._packed_fingerprint = struct.pack(">Q", seabotix_command_t._get_hash_recursive([]))
        return seabotix_command_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


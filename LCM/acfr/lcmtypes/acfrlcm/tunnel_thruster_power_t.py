"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class tunnel_thruster_power_t(object):
    __slots__ = ["utime", "addr", "voltage", "current", "temperature"]

    def __init__(self):
        self.utime = 0
        self.addr = 0
        self.voltage = [ 0.0 for dim0 in range(2) ]
        self.current = [ 0.0 for dim0 in range(2) ]
        self.temperature = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(tunnel_thruster_power_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qb", self.utime, self.addr))
        buf.write(struct.pack('>2d', *self.voltage[:2]))
        buf.write(struct.pack('>2d', *self.current[:2]))
        buf.write(struct.pack(">d", self.temperature))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != tunnel_thruster_power_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return tunnel_thruster_power_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = tunnel_thruster_power_t()
        self.utime, self.addr = struct.unpack(">qb", buf.read(9))
        self.voltage = struct.unpack('>2d', buf.read(16))
        self.current = struct.unpack('>2d', buf.read(16))
        self.temperature = struct.unpack(">d", buf.read(8))[0]
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if tunnel_thruster_power_t in parents: return 0
        tmphash = (0xae0bad201a2d00b) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if tunnel_thruster_power_t._packed_fingerprint is None:
            tunnel_thruster_power_t._packed_fingerprint = struct.pack(">Q", tunnel_thruster_power_t._get_hash_recursive([]))
        return tunnel_thruster_power_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class auv_local_path_t(object):
    __slots__ = ["utime", "num_el", "x", "y", "z"]

    max_num_el = 50

    def __init__(self):
        self.utime = 0
        self.num_el = 0
        self.x = [ 0.0 for dim0 in range(50) ]
        self.y = [ 0.0 for dim0 in range(50) ]
        self.z = [ 0.0 for dim0 in range(50) ]

    def encode(self):
        buf = BytesIO()
        buf.write(auv_local_path_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qb", self.utime, self.num_el))
        buf.write(struct.pack('>50d', *self.x[:50]))
        buf.write(struct.pack('>50d', *self.y[:50]))
        buf.write(struct.pack('>50d', *self.z[:50]))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != auv_local_path_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return auv_local_path_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = auv_local_path_t()
        self.utime, self.num_el = struct.unpack(">qb", buf.read(9))
        self.x = struct.unpack('>50d', buf.read(400))
        self.y = struct.unpack('>50d', buf.read(400))
        self.z = struct.unpack('>50d', buf.read(400))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if auv_local_path_t in parents: return 0
        tmphash = (0x134ffa29f0528ea5) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if auv_local_path_t._packed_fingerprint is None:
            auv_local_path_t._packed_fingerprint = struct.pack(">Q", auv_local_path_t._get_hash_recursive([]))
        return auv_local_path_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

